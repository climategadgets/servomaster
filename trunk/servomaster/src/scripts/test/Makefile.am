## $d$

testbindir = ${prefix}/lib/test/bin
testbin_PROGRAMS = ft_clock ft_swing sync_check
ft_clock_SOURCES = ft_clock
ft_swing_SOURCES = ft_swing
sync_check_SOURCES = sync_check

RM = @RM@

ft_clock:

ft_swing:

sync_check:

clean:

	${RM} -f *~ .*~ ${testbin_PROGRAMS}

